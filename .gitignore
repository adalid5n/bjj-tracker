node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

# Claude — docs internos del proyecto (planes, requisitos, estado, ADRs).
# Se versionan para que viajen con el código entre máquinas.
# Si en algún momento se añaden subcarpetas con contenido sensible o efímero
# (ej. transcripts, agent-reports temporales), exclúyelas aquí explícitamente.

# SQLite-WASM assets (regenerated by scripts/copy-sqlite-wasm.mjs)
/static/sqlite/*
!/static/sqlite/.gitkeep
